let initialState = {
  friendsData: [
    { id: 1, name: 'Ivan' },
    { id: 2, name: 'Alex' },
    { id: 3, name: 'Nastia' },
  ],
};

export const sideBarFriendsReducer = (state= initialState, action) => {

  return state;
};
